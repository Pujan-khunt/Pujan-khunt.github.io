# JavaScript Notes

## Variables

- `var`: Function-scoped or globally-scoped.
- `let`: Block-scoped.
- `const`: Block-scoped and immutable.

## Functions

### Function Declaration

```javascript
function greet(name) {
    return `Hello, ${name}!`;
}
